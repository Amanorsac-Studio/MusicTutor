# Device Support

The current slice can request browser camera and microphone access and render the camera stream. Its routing model represents two cameras, two mono microphones, two stereo keyboard inputs, MIDI, and monitoring.

Native ASIO/WASAPI channel routing, dual-camera capture, MIDI enumeration, hot-plug recovery, and stable identifiers require the JUCE phase and physical hardware validation.
