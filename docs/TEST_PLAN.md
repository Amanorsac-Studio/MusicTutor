# Test Plan

Current gates are strict TypeScript compilation, production bundling, initial Studio rendering, recording activation, workspace navigation, and console-error inspection.

Native gates must add schema, timestamp, ducking-envelope, routing-graph, recording-state, recovery, path-safety, device-churn, and 30/60-minute capture tests from the product specification.
